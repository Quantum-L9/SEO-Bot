node_modules/
dist/
!packages/graphiti-memory-client/dist/
!packages/graphiti-memory-client/dist/**
!packages/llm-router/dist/
!packages/llm-router/dist/**
!packages/infisical-config/dist/
!packages/infisical-config/dist/**
*.log
*.rdb
dump.rdb

# Generated assurance evidence
validation/runs/

# Local validation state
.validation-tmp/

# Governed IDE profile stamps (rewritten locally; do not commit)
.vscode/.l9-ide-desired-hash
.vscode/.l9-agentdocs-hash

# Session-local governance scratch: memory receipts, phase locks, L4 autonomy
# phase/release state, PR handoffs. Carries session identifiers and is rewritten
# per run, so it is never shared branch content. Matches the constellation
# convention (Website-Bot, l9-ci-core, l9-meta-injector, Cognitive.Engine.Graphs).
.l9/

# Governance command/skill adapters projected into the workspace by the
# sessionStart wiring. They are generated copies of the Cursor-Governance SSOT,
# not repository content: committing them would create a second governance tree
# inside a consumer repo. The one hand-authored file here
# (.claude/hooks/session-start.sh) stays tracked.
.claude/commands/
.claude/skills/
